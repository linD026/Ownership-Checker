# deprecated

object relationship graph

---

         #[linked list]
file_info->func_head
|                ||             #[linked list]
| #[function] fso_object(node)->func_args_head
|                       |              ||
|                       |      fso_object(func_args_node)
|                       |
|                       V
|                 #[linked list]
|             func_block_scope_head
|                       ||                       #[linked list]
| #[block] bso_object(func_block_scope_node)->var_declaration_head
|                                                      ||
|                                             bso_object(var_declaration_node)
|
|      #[linked list]
+-------->var_head



---
 
NOTE:
- #[attribute]
- struture(member)

---

the file organization

src dir
- token.c
    - parse the raw word in the text file and generate the token
- parser.c
    - parse the basic C language
- debug.c
    - print the human readable debug info
- fork.c
    - store, fork, and join the function state which also includes the var state
- check_ownership.c
    - check the attribute of pointer is violated the rule
- osc.c
    - parse the input parameters and create the basic file info
